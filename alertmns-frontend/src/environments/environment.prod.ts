/**
 * Variables d'environnement Angular — mode production.
 * Les URLs pointent vers le serveur déployé.
 */
export const environment = {
  production: true,
  apiUrl: 'https://api.alertmns.fr/api',
  wsUrl: 'https://api.alertmns.fr/ws'
};
